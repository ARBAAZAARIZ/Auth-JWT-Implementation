# --- DATABASE SECRETS ---
spring.datasource.url=jdbc:mysql://localhost:3306/los_lms_db
spring.datasource.username=root
#spring.datasource.password=your_db_password

# --- JWT SECRETS ---
# This is the master secret key for signing ALL your tokens.
# Generate a strong, random 64-byte, Base64-encoded string for this.
# You can use an online generator or a command like: openssl rand -base64 64
app.jwt.secret=c3ByaW5nYm9vdHVzaW5nand0b2F1dGh3aXRoc3ByaW5nc2VjdXJpdHk=-

# --- JWT EXPIRATION TIMES ---
# As you requested: 1 minute for Access Token (for testing)
app.jwt.access-token-expiration-ms=60000

# We'll set the Refresh Token to 5 minutes for testing.
# This lets us test the refresh logic multiple times before the refresh token itself expires.
app.jwt.refresh-token-expiration-ms=300000

# --- GOOGLE OAUTH2 SECRETS (We will fill these in later) ---
spring.security.oauth2.client.registration.google.client-id=YOUR_GOOGLE_CLIENT_ID_GOES_HERE
spring.security.oauth2.client.registration.google.client-secret=YOUR_GOOGLE_CLIENT_SECRET_GOES_HERE